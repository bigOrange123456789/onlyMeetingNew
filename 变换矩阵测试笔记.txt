位置*旋转*放缩*坐标
        //测试
        //个人思考矩阵问题是觉得列向量更好理解，所以此时3个0元素在最后一行
        //进行的是行变换
        var mytest=new THREE.Object3D();
        //位置、旋转、放缩 这三个变换在用户看来是独立的//但是对于矩阵乘法来说却有顺序
        //旋转*放缩 *   坐标
        //位置*放缩 *   坐标
        //位置*旋转*放缩*坐标//threeJS中这三个矩阵应该是实时计算的
        //执行顺序缩放，旋转，位移
        //先缩放后旋转的效果，使得旋转矩阵加上缩放效果被‘列变换了’

        //mytest.position.set(1,1,1);
        mytest.scale.set(1,2,3);
        mytest.rotation.set(Math.PI/2,0,0);//顺时针方向旋转
        mytest.updateMatrix();

        console.log(mytest);